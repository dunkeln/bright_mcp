# Required by the published Bright Data MCP. Never commit the real value.
BRIGHTDATA_API_KEY=

# MCPJam routes this pinned model through OpenRouter.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
EVAL_RUNS=20

# Set only after both published endpoints use comparable live providers.
EVAL_PROVIDER_PARITY=demo
